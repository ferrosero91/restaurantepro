# Configuración Docker - RestaurantPro
# Este archivo es un template, será configurado automáticamente

# Base de Datos
DB_HOST=mysql
DB_USER=restaurante_user
DB_PASSWORD=restaurante_secure_password
DB_ROOT_PASSWORD=root_secure_password
DB_NAME=restaurante_saas
DB_POOL_SIZE=20

# Servidor
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Seguridad
SESSION_SECRET=change_this_to_random_64_chars

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Sesión
SESSION_MAX_AGE=86400000

# CORS
ALLOWED_ORIGINS=https://restaurante.app,https://www.restaurante.app

# Uploads
MAX_FILE_SIZE=5242880

# Logs
LOG_LEVEL=info
