<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reporte de Clientes - RestaurantPro</title>
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/vendor/bootstrap-icons/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
        
        .page-header {
            background: white;
            border-radius: 8px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
        }
        
        .table-card {
            background: white;
            border-radius: 8px;
            padding: 1.5rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
        }
        
        .table thead th {
            background: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.875rem;
            padding: 1rem 0.75rem;
            color: #2c3e50;
        }
        
        .table tbody tr:hover {
            background: #f8f9fa;
        }
        
        .badge-rank {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.875rem;
        }
        
        .badge-rank.gold { background: linear-gradient(135deg, #ffd700, #ffed4e); color: #000; }
        .badge-rank.silver { background: linear-gradient(135deg, #c0c0c0, #e8e8e8); color: #000; }
        .badge-rank.bronze { background: linear-gradient(135deg, #cd7f32, #e8a87c); color: #fff; }
        .badge-rank.other { background: #e9ecef; color: #6c757d; }
        
        .cliente-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 1.1rem;
        }
        
        .btn-primary {
            background-color: #3498db;
            border-color: #3498db;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
            border-color: #2980b9;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    
    <main class="py-4">
        <div class="container-fluid px-3 px-md-4">
            
            <div class="page-header">
                <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
                    <div>
                        <h2 class="mb-1" style="font-weight: 700; color: #2c3e50;">
                            <i class="bi bi-people me-2"></i>Reporte de Clientes
                        </h2>
                        <p class="text-muted mb-0">Análisis de mejores clientes</p>
                    </div>
                    <a href="/reportes" class="btn btn-outline-primary">
                        <i class="bi bi-arrow-left me-2"></i>Volver al Dashboard
                    </a>
                </div>
            </div>

            <div class="table-card">
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr>
                                <th width="60">Ranking</th>
                                <th>Cliente</th>
                                <th>Contacto</th>
                                <th class="text-end">Total Compras</th>
                                <th class="text-end">Ticket Promedio</th>
                                <th class="text-end">Total Gastado</th>
                                <th>Última Compra</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (clientes.length === 0) { %>
                                <tr>
                                    <td colspan="7" class="text-center text-muted py-5">
                                        <i class="bi bi-inbox" style="font-size: 3rem; opacity: 0.3;"></i>
                                        <p class="mt-3">No hay datos de clientes en el período seleccionado</p>
                                    </td>
                                </tr>
                            <% } else { %>
                                <% clientes.forEach((cliente, index) => { %>
                                    <tr>
                                        <td>
                                            <span class="badge-rank <%= index === 0 ? 'gold' : index === 1 ? 'silver' : index === 2 ? 'bronze' : 'other' %>">
                                                <%= index + 1 %>
                                            </span>
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center gap-2">
                                                <div class="cliente-avatar">
                                                    <%= cliente.nombre.charAt(0).toUpperCase() %>
                                                </div>
                                                <strong><%= cliente.nombre %></strong>
                                            </div>
                                        </td>
                                        <td>
                                            <% if (cliente.telefono) { %>
                                                <i class="bi bi-telephone me-1"></i><%= cliente.telefono %>
                                            <% } else { %>
                                                <span class="text-muted">Sin teléfono</span>
                                            <% } %>
                                        </td>
                                        <td class="text-end">
                                            <span class="badge bg-primary"><%= cliente.total_compras %></span>
                                        </td>
                                        <td class="text-end">
                                            $<%= Number(cliente.ticket_promedio).toLocaleString('es-CO', {minimumFractionDigits:2}) %>
                                        </td>
                                        <td class="text-end">
                                            <strong class="text-success">$<%= Number(cliente.total_gastado).toLocaleString('es-CO', {minimumFractionDigits:2}) %></strong>
                                        </td>
                                        <td>
                                            <small class="text-muted">
                                                <%= new Date(cliente.ultima_compra).toLocaleDateString('es-CO', {year: 'numeric', month: 'short', day: 'numeric'}) %>
                                            </small>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </main>

    <%- include('../partials/footer') %>
    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
