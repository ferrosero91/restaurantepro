<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reporte de Productos - RestaurantPro</title>
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/vendor/bootstrap-icons/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        main {
            flex: 1 0 auto;
        }
        
        footer {
            flex-shrink: 0;
        }
        
        .page-header {
            background: white;
            border-radius: 8px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
        }
        
        .table-card {
            background: white;
            border-radius: 8px;
            padding: 1.5rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
        }
        
        .table thead th {
            background: #f8f9fa;
            border-bottom: 2px solid #dee2e6;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.875rem;
            padding: 1rem 0.75rem;
            color: #2c3e50;
        }
        
        .table tbody tr:hover {
            background: #f8f9fa;
        }
        
        .badge-rank {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.875rem;
        }
        
        .badge-rank.gold { background: linear-gradient(135deg, #ffd700, #ffed4e); color: #000; }
        .badge-rank.silver { background: linear-gradient(135deg, #c0c0c0, #e8e8e8); color: #000; }
        .badge-rank.bronze { background: linear-gradient(135deg, #cd7f32, #e8a87c); color: #fff; }
        .badge-rank.other { background: #e9ecef; color: #6c757d; }
        
        .progress {
            height: 8px;
            border-radius: 4px;
        }
        
        .btn-primary {
            background-color: #3498db;
            border-color: #3498db;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
            border-color: #2980b9;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    
    <main class="py-4">
        <div class="container-fluid px-3 px-md-4">
            
            <div class="page-header">
                <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
                    <div>
                        <h2 class="mb-1" style="font-weight: 700; color: #2c3e50;">
                            <i class="bi bi-box-seam me-2"></i>Reporte de Productos
                        </h2>
                        <p class="text-muted mb-0">Análisis de productos más vendidos</p>
                    </div>
                    <a href="/reportes" class="btn btn-outline-primary">
                        <i class="bi bi-arrow-left me-2"></i>Volver al Dashboard
                    </a>
                </div>
            </div>

            <div class="table-card">
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr>
                                <th width="60">Ranking</th>
                                <th>Código</th>
                                <th>Producto</th>
                                <th class="text-end">Cantidad Vendida</th>
                                <th class="text-end">Veces Vendido</th>
                                <th class="text-end">Total Vendido</th>
                                <th width="200">Participación</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (productos.length === 0) { %>
                                <tr>
                                    <td colspan="7" class="text-center text-muted py-5">
                                        <i class="bi bi-inbox" style="font-size: 3rem; opacity: 0.3;"></i>
                                        <p class="mt-3">No hay datos de productos en el período seleccionado</p>
                                    </td>
                                </tr>
                            <% } else { %>
                                <% 
                                const totalVentas = productos.reduce((sum, p) => sum + Number(p.total_vendido), 0);
                                productos.forEach((producto, index) => { 
                                    const participacion = (Number(producto.total_vendido) / totalVentas * 100).toFixed(1);
                                %>
                                    <tr>
                                        <td>
                                            <span class="badge-rank <%= index === 0 ? 'gold' : index === 1 ? 'silver' : index === 2 ? 'bronze' : 'other' %>">
                                                <%= index + 1 %>
                                            </span>
                                        </td>
                                        <td><code><%= producto.codigo %></code></td>
                                        <td><strong><%= producto.nombre %></strong></td>
                                        <td class="text-end"><%= Number(producto.cantidad_vendida).toFixed(2) %></td>
                                        <td class="text-end"><span class="badge bg-info"><%= producto.veces_vendido %></span></td>
                                        <td class="text-end"><strong>$<%= Number(producto.total_vendido).toLocaleString('es-CO', {minimumFractionDigits:2}) %></strong></td>
                                        <td>
                                            <div class="d-flex align-items-center gap-2">
                                                <div class="progress flex-grow-1">
                                                    <div class="progress-bar bg-success" style="width: <%= participacion %>%"></div>
                                                </div>
                                                <small class="text-muted" style="min-width: 45px;"><%= participacion %>%</small>
                                            </div>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </main>

    <%- include('../partials/footer') %>
    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
