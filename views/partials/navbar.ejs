<nav class="navbar navbar-expand-lg navbar-dark mb-4" style="background-color:#2c3e50;box-shadow:0 2px 4px rgba(0,0,0,.1);font-size:1.1rem;">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/" style="color:#fff;font-size:1.4rem;">
      <i class="bi bi-shop me-2"></i>RestaurantPro
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% 
        // Helper para verificar permisos
        const hasPermission = (ruta) => {
          if (!user) return false;
          if (user.rol === 'superadmin') return true;
          return user.permisosRutas && user.permisosRutas.includes(ruta);
        };
        %>
        
        <% if (hasPermission('/')) { %>
        <li class="nav-item">
          <a href="/" class="nav-link" data-bs-toggle="tooltip" title="Facturación">
            <i class="bi bi-house-fill me-1"></i>Inicio
          </a>
        </li>
        <% } %>
        
        <% if (hasPermission('/mesas')) { %>
        <li class="nav-item">
          <a href="/mesas" class="nav-link" data-bs-toggle="tooltip" title="Mesas">
            <i class="bi bi-grid me-1"></i>Mesas
          </a>
        </li>
        <% } %>
        
        <% if (hasPermission('/cocina')) { %>
        <li class="nav-item">
          <a href="/cocina" class="nav-link" data-bs-toggle="tooltip" title="Cocina">
            <i class="bi bi-egg-fried me-1"></i>Cocina
          </a>
        </li>
        <% } %>
        
        <% if (hasPermission('/reportes')) { %>
        <li class="nav-item">
          <a href="/reportes" class="nav-link" data-bs-toggle="tooltip" title="Reportes">
            <i class="bi bi-bar-chart-line me-1"></i>Reportes
          </a>
        </li>
        <% } %>
        
        <% if (hasPermission('/productos')) { %>
        <li class="nav-item">
          <a href="/productos" class="nav-link" data-bs-toggle="tooltip" title="Productos">
            <i class="bi bi-box me-1"></i>Productos
          </a>
        </li>
        <% } %>
        
        <% if (hasPermission('/configuracion')) { %>
        <li class="nav-item">
          <a href="/configuracion" class="nav-link" data-bs-toggle="tooltip" title="Configuración">
            <i class="bi bi-gear me-1"></i>Config
          </a>
        </li>
        <% } %>
        
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
            <i class="bi bi-person-circle me-1"></i>Cuenta
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="/logout">
              <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
            </a></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

